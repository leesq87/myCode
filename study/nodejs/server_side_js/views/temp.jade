html
  head
    title= _title
  body
    h1 Hello jade
    ul
      -for(var i=0; i<5; i++)
        li coding
    ul jade에서 동적인 문법을 사용하기위해서는 마이너스부호(-)를 넣어서 사용한다.
    dev= time
    li =을 넣으면 변수를 넣는것이다.
    li jade 밖에서 변수를 선언해 주고, 넣어줘야한다.
